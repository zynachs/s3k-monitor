#include <stdbool.h>
#include <string.h>

#include "s3k.h"

#define ALIGNED(x)    __attribute__((aligned(x)))
#define ARRAY_SIZE(x) ((sizeof(x)) / (sizeof(x[0])))

#define MONITOR_PID 0
#define APP_PID 1

void setup(void)
{
}

void loop(void)
{
}
